---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tagbio-firewall-network-policy
  namespace: {{ .Release.namespace }}
spec:
  podSelector:
    matchLabels:
      firewall.tag.bio/role: protected
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              firewall.tag.bio/role: gateway
          namespaceSelector:
            matchLabels:
              firewall.tag.bio/role: gateway
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tagbio-firewall-network-policy
  namespace: tagbio-app
spec:
  podSelector:
    matchLabels:
      firewall.tag.bio/role: protected
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              firewall.tag.bio/role: gateway
          namespaceSelector:
            matchLabels:
              firewall.tag.bio/role: gateway
