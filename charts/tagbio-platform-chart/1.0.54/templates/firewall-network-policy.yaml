# Commented until I figure out how to make it work. -kenn
# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: tagbio-firewall-network-policy
#   namespace: tagbio-app
# spec:
#   podSelector:
#     matchLabels:
#       firewall.tag.bio/role: protected
#   policyTypes:
#     - Ingress
#   ingress:
#     - from:
#         - podSelector:
#             matchLabels:
#               firewall.tag.bio/role: gateway
#           namespaceSelector:
#             matchLabels:
#               firewall.tag.bio/role: gateway
